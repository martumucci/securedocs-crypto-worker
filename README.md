# securedocs-crypto-worker

Worker that consumes document submission events, fetches the payload, encrypts it with AES-256-GCM, signs the hash with Ed25519, and publishes the result back to the API.

Part of the SecureDocs system. This README will be filled in once the implementation is complete.
